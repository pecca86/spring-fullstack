version: "3.8"
services:
  backend:
    image: "pecca86/springboot-react-fullstack:manual_push"
    ports:
      - "80:8080"
    restart: "always"
    environment:
      SPRING_PROFILES_ACTIVE: "aws"
